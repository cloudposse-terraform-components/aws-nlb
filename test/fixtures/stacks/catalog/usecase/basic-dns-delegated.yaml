components:
  terraform:
    nlb/dns-delegated:
      metadata:
        component: target
      vars:
        enabled: true
        dns_acm_enabled: true
        name: "nlb"
        attributes:
          - "dns-delegated"